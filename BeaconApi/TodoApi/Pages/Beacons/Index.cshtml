@page
@model BeaconApi.Pages.Beacons.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BeaconItem[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BeaconItem[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BeaconItem[0].ExtraInfo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BeaconItem) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtraInfo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BeaconId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BeaconId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BeaconId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
